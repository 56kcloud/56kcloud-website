import CardImage from '../molecules/card-image'

export default function CardsImage ({cards, theme = 'dark'}) {
  console.log(cards[].)
  return (
    <section className={'section-padding bg-blue-lighter'}>
      {/* {cards.map((card) => (
        <CardImage
          key={card.title}
          number={card.number}
          image={card.image}
          icon={card.icon}
          title={card.title}
          description={card.description}
          alignment={card.alignment}
          theme='light' />
      ))} */}
    </section>
  )
}
